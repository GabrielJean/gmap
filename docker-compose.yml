services:
  gmap:
    build: .
    container_name: gmap
    volumes:
      - /Apps/gmap:/app/data_runs
    ports:
      - "5000"  # Expose container port 5000 on a random host port
    restart: unless-stopped
